streamlit>=1.40,<2
faiss-cpu>=1.8.0
sentence-transformers>=3.0,<6
PyMuPDF>=1.24
groq>=0.13
numpy>=1.26
pandas>=2.0
# NEW: only used for the optional PDF export (pure Python, ~1 MB). The app still starts without it.
fpdf2>=2.7.9
